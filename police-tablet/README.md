# POLIS – React NUI Frontend

React-Frontend für die FiveM-Resource. Build-Output landet in `../html/`.

## Entwicklung

```bash
npm install
npm run dev        # Browser-Entwicklung
npm run build:fivem # Build für FiveM NUI → ../html/
```

Siehe [Root-README](../README.md) für FiveM-Installation.
